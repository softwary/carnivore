import { SplitLettersPipe } from './split-letters.pipe';

describe('SplitLettersPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitLettersPipe();
    expect(pipe).toBeTruthy();
  });
});
